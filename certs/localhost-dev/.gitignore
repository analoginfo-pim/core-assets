# `openssl x509 -CAcreateserial` writes the next-serial ledger here.
# It's regenerated on every regenerator run and has no value to
# downstream consumers (manifest.json carries everything they need).
dev-ca.srl
